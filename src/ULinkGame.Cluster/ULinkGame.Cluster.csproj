<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <ImplicitUsings>disable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <RootNamespace>ULinkGame.Cluster</RootNamespace>
    <PackageId>ULinkGame.Cluster</PackageId>
    <Version>0.1.0</Version>
    <Description>Optional explicit cluster routing contracts and in-memory runtime primitives for ULinkGame.</Description>
    <PackageTags>ulinkgame;game;cluster;routing;actor</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="System.Diagnostics.DiagnosticSource" Version="10.0.0" />
  </ItemGroup>

  <ItemGroup>
    <None Update="README.md" Pack="true" PackagePath="\" />
  </ItemGroup>
</Project>
