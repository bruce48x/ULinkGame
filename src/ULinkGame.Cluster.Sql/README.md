# ULinkGame.Cluster.Sql

SQL-backed node directory persistence for ULinkGame cluster membership.

Applications provide their own `DbConnection` factory and SQL provider package. This package contains only provider-neutral node-directory persistence code.
